<!-- This is how you include a partial, aka a fragment of html to include on many pages
	Note: you can pass in local variables so that partials can render various items from various pages.
	-->
<%= partial :_nav_bar, :locals=>{:location=>'Home'} %>